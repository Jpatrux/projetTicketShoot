<form method="post" action="{{ path('app_shootarounds_delete', {'id': shootaround.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shootaround.id) }}">
    <button class="btn">Delete</button>
</form>
